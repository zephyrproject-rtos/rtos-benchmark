if (CONFIG_ARM)
    add_subdirectory(arm)
elseif (CONFIG_X86)
    add_subdirectory(x86)
elseif (CONFIG_RISCV)
    add_subdirectory(riscv)
else()
    message(FATAL_ERROR "Unknown Zephyr architecture!")
endif()
